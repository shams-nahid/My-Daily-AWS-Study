## Caching Strategies

- `Cloudfront`
  - `Edge` level caching
  - Closest to the user
- `API Gateway`
  - `Region` level caching
- DB Caching (`Application` Level)
  - `Redis` (`Elastic Cache`)
  - `Memcached` (`Elastic Cache`)
  - `DAX`
